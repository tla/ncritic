write tests for gap detection and 2-word glommed matching
figure out how to handle orthography variation
figure out how to handle paragraph / div splitting
make spelling corrections alter the number of decisions needed on the fly
just generally pull XML into fewer data structures, maybe?
figure out some way to cope with blank #text elements
allow reading group assignments on the command line
safeguards against selecting something twice
ability to back up
make "note" an allowed apparatus child
have a filter for what sort of punctuation we care about.  Or just use it more effectively.
write a rule for capital / lowercase orthography
need a way to mark a witDetail as "use this", rather than consuming it.
...Should definitely put it in a <c type="punct"/> container.
handle catchwords appropriately (i.e. don't collate them.)
